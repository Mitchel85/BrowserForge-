# BrowserForge-
A serverless, browser-native AI agent (single-file HTML). It reads and manipulates code, images, and Office files directly on your local drive and connects flexibly to local models (Ollama) or cloud APIs like OpenAI and Gemini.
